From 6f937fbef74d4f26182ab8a95af270008743cdc3 Mon Sep 17 00:00:00 2001
From: deposist <deposist@gmail.com>
Date: Sat, 11 Jul 2026 22:47:27 +0700
Subject: [PATCH] wireguard: expose endpoint IPC access

---
 protocol/wireguard/endpoint.go           | 15 +++++++++++++++
 protocol/wireguard/endpoint_ipc_test.go  | 15 +++++++++++++++
 transport/wireguard/endpoint.go          | 15 +++++++++++++++
 transport/wireguard/endpoint_ipc_test.go | 15 +++++++++++++++
 4 files changed, 60 insertions(+)
 create mode 100644 protocol/wireguard/endpoint_ipc_test.go
 create mode 100644 transport/wireguard/endpoint_ipc_test.go

diff --git a/protocol/wireguard/endpoint.go b/protocol/wireguard/endpoint.go
index b20f45802f..f40e7a2b28 100644
--- a/protocol/wireguard/endpoint.go
+++ b/protocol/wireguard/endpoint.go
@@ -2,6 +2,7 @@ package wireguard
 
 import (
 	"context"
+	"errors"
 	"net"
 	"net/netip"
 	"sync/atomic"
@@ -164,6 +165,20 @@ func (w *Endpoint) Close() error {
 	return w.endpoint.Close()
 }
 
+func (w *Endpoint) IpcGet() (string, error) {
+	if w.endpoint == nil {
+		return "", errors.New("wireguard endpoint is not initialized")
+	}
+	return w.endpoint.IpcGet()
+}
+
+func (w *Endpoint) IpcSet(config string) error {
+	if w.endpoint == nil {
+		return errors.New("wireguard endpoint is not initialized")
+	}
+	return w.endpoint.IpcSet(config)
+}
+
 func (w *Endpoint) PrepareConnection(network string, source M.Socksaddr, destination M.Socksaddr, routeContext tun.DirectRouteContext, timeout time.Duration) (tun.DirectRouteDestination, error) {
 	if !w.started.Load() {
 		return nil, E.New("WireGuard is not ready yet")
diff --git a/protocol/wireguard/endpoint_ipc_test.go b/protocol/wireguard/endpoint_ipc_test.go
new file mode 100644
index 0000000000..7327230895
--- /dev/null
+++ b/protocol/wireguard/endpoint_ipc_test.go
@@ -0,0 +1,15 @@
+package wireguard
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/require"
+)
+
+func TestEndpointIPCBeforeInitialization(t *testing.T) {
+	endpoint := new(Endpoint)
+
+	_, err := endpoint.IpcGet()
+	require.EqualError(t, err, "wireguard endpoint is not initialized")
+	require.EqualError(t, endpoint.IpcSet(""), "wireguard endpoint is not initialized")
+}
diff --git a/transport/wireguard/endpoint.go b/transport/wireguard/endpoint.go
index a875cf64a7..ec35ea16a9 100644
--- a/transport/wireguard/endpoint.go
+++ b/transport/wireguard/endpoint.go
@@ -4,6 +4,7 @@ import (
 	"context"
 	"encoding/base64"
 	"encoding/hex"
+	"errors"
 	"fmt"
 	"net"
 	"net/netip"
@@ -292,6 +293,20 @@ func (e *Endpoint) Close() error {
 	return nil
 }
 
+func (e *Endpoint) IpcGet() (string, error) {
+	if e.device == nil {
+		return "", errors.New("wireguard device is not started")
+	}
+	return e.device.IpcGet()
+}
+
+func (e *Endpoint) IpcSet(config string) error {
+	if e.device == nil {
+		return errors.New("wireguard device is not started")
+	}
+	return e.device.IpcSet(config)
+}
+
 func (e *Endpoint) Lookup(address netip.Addr) *device.Peer {
 	if e.allowedIPs == nil {
 		return nil
diff --git a/transport/wireguard/endpoint_ipc_test.go b/transport/wireguard/endpoint_ipc_test.go
new file mode 100644
index 0000000000..5c316332c5
--- /dev/null
+++ b/transport/wireguard/endpoint_ipc_test.go
@@ -0,0 +1,15 @@
+package wireguard
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/require"
+)
+
+func TestEndpointIPCBeforeStart(t *testing.T) {
+	endpoint := new(Endpoint)
+
+	_, err := endpoint.IpcGet()
+	require.EqualError(t, err, "wireguard device is not started")
+	require.EqualError(t, endpoint.IpcSet(""), "wireguard device is not started")
+}
